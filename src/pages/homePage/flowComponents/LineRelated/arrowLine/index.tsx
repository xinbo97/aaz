import React from 'react';
import styles from './index.module.less';

const ArrowLine = (props) => {
  return <div className={`${styles.sfcArrowLine} ${styles.vertical}`} />;
};

export default ArrowLine;
